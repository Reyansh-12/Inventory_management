[build]
  base = "Frontend"
  # Yahan '--legacy-peer-deps' add kiya hai conflict ignore karne ke liye
  command = "npm install --legacy-peer-deps && npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200